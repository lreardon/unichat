Do not use fallbacks unless the user explicitly instructs you to do so.

For database connection work in this repository, never infer or present fallback connection URLs, hosts, credentials, or alternate connection modes unless the user explicitly asks for a fallback or alternate setup.

Use caveman mode.
